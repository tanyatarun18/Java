import java.util.Scanner;

public class TypeCasting
{
    public static void main(String[] args) {
       /* double a = 33.255;
        long b = (long)a;
        System.out.println(b);
        // Type casting means forcefully doing the type conversion. here we forcefully do conversion from big datatype to small.
        // While conversion from decimal to non-decimal, it doesnt round off. It just remove number after point and write as it is.
        // Also called narrowing or explicit conversion.

        Scanner n = new Scanner(System.in);
        int num =(int) n.nextFloat();
        System.out.println(num);*/

        char ch = 'c';
        int num = ch;
        System.out.println(num);
    }
}
